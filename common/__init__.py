"""Mini-Dynamo shared library.

Reusable building blocks shared across the router, prefill, and decode
services: configuration, schemas, simulated GPU memory, the mock model
backend, KV-transfer cost model, continuous batching, telemetry and metrics.
"""

__version__ = "0.1.0"
